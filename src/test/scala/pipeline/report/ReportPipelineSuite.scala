package it.scarpenti.marioinc
package pipeline.report


import com.holdenkarau.spark.testing.DataFrameSuiteBase
import org.scalatest.funsuite.AnyFunSuite

class ReportPipelineSuite extends AnyFunSuite with DataFrameSuiteBase {

  test("groupByMonthAndArea should aggregate records"){

    /*
    val sampleData = [Generate some data to pass to ReportPipeline.groupByMonthAndArea]
    val report = ReportPipeline.groupByMonthAndArea(sampleData)

    Check that the result is as expected. i.e:
    - number of records corresponds to the number of distinct pair (month, area)
    - possibly we can also check the desired schema
    * */

  }

}
